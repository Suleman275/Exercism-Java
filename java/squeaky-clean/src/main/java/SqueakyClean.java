class SqueakyClean {
    static String clean(String identifier) {
        // throw new UnsupportedOperationException("Please implement the (static) SqueakyClean.clean() method");

        identifier = identifier.replaceAll(" ", "-"); //Replace all spaces with dashes

        // char[identifier.length()] charsa = identifier.toCharArray();
        Character.

        return clean;
    }
}
